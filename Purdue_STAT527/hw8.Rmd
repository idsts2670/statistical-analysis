---
title: "STAT 527 HW 8"
author: "Satoshi Ido (ID: 34788706)"
date: 10 April 2023
output: pdf_document
---
```{r setup, include = FALSE, cache = FALSE}
knitr::opts_chunk$set(
    echo = TRUE,
    warning = FALSE,
    message = FALSE)
```

```{r}
# data prep
library("lattice")
library("dplyr")
library("stringr")
# get the data from R file
source('Purdue_STAT527/hw8.R')
```

# Q1
```{r}
# extract all the words that start with “s” from the sentence
## https://www.tohoho-web.com/ex/regexp.html#lookaround_assertion
str_extract_all(v1, "(?<=\\b)s[^\\s]+")
```

Q2
```{r}
x <- as.numeric(gsub(".*?([0-9]+).*", "\\1", v2))
x[!is.na(x)]
```

Q3
```{r}
gsub('cat', 'dog', v3)
```